public class GCD_of_num {
    public static int gcdvalue(int n1, int n2) {
        if (n2 == 0) {
            return n1;
        }

        return gcdvalue(n2, n1 % n2);
    }

    public static void main(String[] args) {
        int n1 = 18;
        int n2 = 48;
        int value = gcdvalue(n1, n2);
        System.out.println("The GCD is "+value);
    }
}

The GCD is 6
