////this is leetocde question where input can be anything uppercase lowercase, number mixed  this handle all the case ///






class Solution {
    public boolean isPalindrome(String str) {
        String newString = str.replaceAll("[^A-Za-z0-9]", "").toLowerCase(); //this will  replace space with letter and 
                                                                             tolowercase convet letter into lowercase
        int start = 0;
        int end = newString.length() - 1;
        while (start < end) {
            
            if (newString.charAt(start) != newString.charAt(end)) {

                return false;
            }
            start++;
            end--;
        }
        return true;
    }
}




/////////this code is for normal string ie all string small or capital Input ////////////////////// ////////////


public class pallendrome {
    public static boolean pallendromestring(String str) {
        int start = 0;
        int end = str.length() - 1;
        while (start < end) {
            if (str.charAt(start) != str.charAt(end)) {
                return false;
            }
            start++;
            end--;

        }

        return true;
    }

    public static void main(String[] args) {
        String str = "noon";
        Boolean check = pallendromestring(str);
        System.out.println("pallendrome exist : " + check);
       
    }
}

output:-------------
pallendrome exist : true


Time Complexity: O(n)
Space Complexity: O(1
